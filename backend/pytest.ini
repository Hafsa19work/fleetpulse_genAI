[pytest]
testpaths = tests
python_files = test_*.py
addopts =
    -q
    --strict-markers
    --cov=app
    --cov-report=term-missing
    --cov-report=html:htmlcov
filterwarnings =
    error::DeprecationWarning:app.*
